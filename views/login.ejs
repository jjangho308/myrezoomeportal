<%- include('base_head_import') -%>
<%- include('base_all_dialog') -%>

<head>
    <script src="js/login.js"></script>

    <link rel="stylesheet" href="css/navbar-default.css">

    <link rel="stylesheet" href="css/login.css">

    <script>

        $(document).ready(function () {

        });

        function login() {
            var id = $("#id").val();
            var pw = $("#pw").val();

            var param = {
                userid: id,
                password: pw,
                cmd: 'Login'
            };

            $.ajax({
                url: '/',
                type: 'post',
                //data: $("form").serialize(),
                data: param,
                success: function (data) {
                    //alert(JSON.stringify(data));
                    //window.location.replace('/main?token=' + data.token);
                    sessionStorage.setItem("token", data.token);
                    window.location.href = '/main';
                }
            });

            // alert($("form").serialize());
            // console.log($("form").serialize());

            // $.ajax({
            //     url: '/',
            //     type: 'post',
            //     data: param,
            //     success: function (data) {
            //         alert(JSON.stringify(data));
            //         //window.location.replace('/main?token=' + data.token);
            //         sessionStorage.setItem("token", data.token);
            //         window.location.href = '/main';
            //     }
            // });
        }

    </script>
</head>

<body>
<%- include('base_header_default') -%>

<h1>개인회원 로그인</h1>

<div class="login-container">
    <p>이메일</p>
    <input type="email" placeholder="이메일을 입력해주세요.">

    <p>비밀번호</p>
    <input type="password" placeholder="비밀번호를 입력해주세요.">
    <div class="error-message">ID또는 비밀번호가 잘못 입력되었습니다.</div>

    <button class="login-button">로그인</button>

    <div class="signup">
        <a href="/signup">회원가입</a>
    </div>
    <div class="find-password-div">
        <a href="#find-password-dialog" rel="modal:open" >비밀번호 찾기</a>
    </div>
</div>



<div id="find-password-dialog" class="modal">
    <p>비밀번호 찾기<span>회원가입시 등록한 정보로 비밀번호 변경 링크를 전달해드립니다.</span></p>
    <div class="modal-body">

        <div class= "before-div">
            <div class = "last-name">
                <p>성</p>

                <input type="text"  placeholder="홍">

            </div>
            <div class = "first-name">
                <p>이름</p>

                <input type="text" placeholder="길동">

            </div>


            <p>이메일</p>
            <input type="text"  placeholder="회원가입시 입력한 이메일을 입력해주세요.">

            <div class="error-message">정확한 이메일을 입력해주세요.</div>
        </div>
        <div class= "after-div">
            <h4>suji.lee@email.com</p>
                <p>비밀번호 재설정을 위한 링크를 전송하였습니다. <br>이메일을 확인해주세요.</p>
        </div>
    </div>
    <div class="modal-footer">
        <button class="confirm-btn primary-solid-btn">확인</button>
        <button class="cancel-btn border-btn">취소</button>
    </div>
</div>



</body>

</html>