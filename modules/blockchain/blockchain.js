import AbstractManager from '../abstract_manager';

/**
 * Blockchain manager. <br />
 * 
 * @since 180313
 * @author TACKSU
 */
class BlockChainManager extends AbstractManager{
    constructor(opt){
        super(opt);
    }

    init(){
        super.init();
    }
}

export default BlockChainManager;