import AbstractManager from "../abstract_manager";

class SocketManager extends AbstractManager{

    constructor(opt){
        super(opt);
    }

    sendMessage(socketMessageEntity, done){
        
    }
}